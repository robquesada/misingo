<%= render 'shared/header' %>
<% heading = (@pet_report.class == Adoption) ? 'Mascotas en adopción' : 'Mascotas desaparecidas' %>
<%= render 'shared/page_heading', { report_class: @pet_report.class } %>

<div class="row search">
  <div class="col-md-6 col-md-offset-4">
    <fieldset>
      <%= form_tag(root_path, method: 'get') do %>
        <div class="col-md-6">
          <%= collection_select(:province, :province_id, Province.all, :id, :name,
          { include_blank: 'Provincias', selected: @province_id }, { class: 'form-control' }) %>
        </div>
        <div class="col-md-6">
          <%= button_tag('Buscar', class: 'btn btn-sm btn-default search') %>
        </div>
      <% end %>
    </fieldset>
  </div>
</div>
